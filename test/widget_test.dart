import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MindGate app starts', (WidgetTester tester) async {
    // Basic test - we'll add real tests later
    expect(true, isTrue);
  });
}
